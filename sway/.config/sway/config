include ~/MKDebian/grayscale
exec_always autotiling

# target                 title     bg    text   indicator  border
client.focused           $text     $text $text  $text      $text   
#client.focused_inactive  $overlay0 $base $text  $rosewater $overlay0
client.unfocused         $filling $filling $text  $filling $filling
client.urgent            $red     $red  $red   $red       $red
#client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
client.background        $background

# bar
bar {
  swaybar_command waybar
  }
}

font pango:monospace 0
titlebar_padding 1
titlebar_border_thickness 0

include /etc/sway/config-vars.d/*
include /etc/sway/config.d/*

output * bg /home/mkuntz/Pictures/bkg-fw.jpg fill


exec swayidle -w \
	timeout 270 $lock \
	timeout 300 'swaymsg "output * dpms off"' \
		resume 'swaymsg "output * dpms on"' \
	before-sleep $lock


# Notification Daemon
exec swaync

# Emote Startup
exec emote

# polkit
exec /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1

include ~/.config/sway/monitors
include ~/.config/sway/inputs
include ~/.config/sway/modes
include ~/.config/sway/keybinds
